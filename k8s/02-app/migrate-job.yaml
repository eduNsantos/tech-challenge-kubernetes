apiVersion: batch/v1
kind: Job
metadata:
  name: app-migrate
  namespace: postech
  labels:
    app: postech-app
    component: migrate
spec:
  backoffLimit: 3
  template:
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      restartPolicy: OnFailure
      initContainers:
        - name: wait-for-mysql
          image: busybox:1.36
          envFrom:
            - configMapRef:
                name: app-config
          command:
            - sh
            - -c
            - |
              echo "Aguardando MySQL em ${DB_HOST}:${DB_PORT}...";
              until nc -z "${DB_HOST}" "${DB_PORT}"; do
                sleep 3;
              done;
              echo "MySQL disponivel."
      containers:
        - name: migrate
          image: ghcr.io/edunsantos/tech-challenge-application:latest
          imagePullPolicy: Always
          command:
            - php
            - artisan
            - migrate
            - --force
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "200m"
              memory: "512Mi"